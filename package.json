{
  "name": "controlled-promise-list",
  "version": "1.0.0",
  "description": "Run Async/Promise functions with controlled concurrency",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "files": [
    "dist/"
  ],
  "author": "Ravidhu Dissanayake",
  "license": "MIT",
  "keywords": [
    "promise",
    "limit",
    "limited",
    "concurrency",
    "throttle",
    "throat",
    "rate",
    "batch",
    "ratelimit",
    "task",
    "queue",
    "async",
    "await",
    "promises",
    "bluebird",
    "parallelism",
    "then"
  ],
  "scripts": {
    "dev": "rm -rf dist/* && nodemon",
    "test": "mocha --require tests/typescript.js --watch-extensions ts --timeout 30000 'tests/**/*.ts'",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig-cjs.json",
    "prepare": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chai": "^4.2.12",
    "@types/mocha": "^8.0.3",
    "@typescript-eslint/eslint-plugin": "^1.12.0",
    "@typescript-eslint/parser": "^1.12.0",
    "chai": "^4.2.0",
    "eslint": "^7.7.0",
    "mocha": "^8.1.3",
    "ts-node": "^9.0.0",
    "typescript": "^3.3.3"
  }
}
